/**
 * @Imports
 */
 import styles from '../styles/TimeSlots.module.css';

 // PLAN FIRST:
 // Create event listeners for hovering mouse on/off
 // Need to make the times and boxes for them
 const TimeSlot = () => {
    return (
        <div>
            Group Availability
        </div>
    )
 }

 export default TimeSlot;

